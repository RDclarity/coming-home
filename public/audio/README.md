# Hintergrundmusik

Lege hier die von Jasmin bereitgestellte Datei als **`theme.mp3`** ab
(zusätzlich gern als `theme.m4a` für bessere Safari/iOS-Kompatibilität,
wird automatisch mitgeladen, falls vorhanden).

Der Player in `src/components/MusicPlayer.tsx` ist fertig verdrahtet und
braucht nach dem Ablegen der Datei keine Codeänderung.
